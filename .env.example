# Exemplo de arquivo .env para configuração do projeto
# Este projeto usa a biblioteca spring-dotenv para carregar variáveis de ambiente
# do arquivo .env. Para usar, copie este arquivo para .env e ajuste os valores conforme necessário.
#
# No application.properties, as variáveis são acessadas usando ${NOME_DA_VARIAVEL}
# No docker-compose.yml, as variáveis são acessadas usando ${NOME_DA_VARIAVEL}

# Variáveis de ambiente para application.properties
SPRING_APPLICATION_NAME=xpto

# Oracle DB settings
SPRING_DATASOURCE_URL=jdbc:oracle:thin:@//localhost:1521/XEPDB1
SPRING_DATASOURCE_USERNAME=admin
SPRING_DATASOURCE_PASSWORD=admin123
SPRING_DATASOURCE_DRIVER_CLASS_NAME=oracle.jdbc.OracleDriver

# JPA/Hibernate settings
SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.OracleDialect
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=true

# Variáveis de ambiente para docker-compose.yml
DB_IMAGE=gvenzl/oracle-xe:21-slim
DB_CONTAINER_NAME=oracle-xe
DB_PORT=1521
DB_ENTERPRISE_MANAGER_PORT=5500
ORACLE_PASSWORD=admin123
APP_USER=admin
APP_USER_PASSWORD=admin123

DBEAVER_IMAGE=dbeaver/cloudbeaver:latest
DBEAVER_CONTAINER_NAME=cloudbeaver
DBEAVER_PORT=8978